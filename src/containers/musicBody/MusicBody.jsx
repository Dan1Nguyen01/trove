import React from "react";

export default function MusicBody () {
    const [music,setMusic]  = React.useState([]);

    React.useEffect(()=>{
        fetch(()=>{
            
        })
    })

    return(
        <div>
            <h1>Music Slide</h1>
        </div>
    )
}